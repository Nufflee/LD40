using System;
using UnityEngine;

public class NoElectricityPopUp : MonoBehaviour
{
  public Action onAction;
  
  // Use this for initialization
  void Start()
  {
  }

  // Update is called once per frame
  void Update()
  {
  }
}